# 2026-08-28

## gstack design qa — 经营 API 仪表盘 (Boulangerie · Ledger)

跑了一遍 `/gstack design qa` 流程，对 http://127.0.0.1:8600 做标准深度审计。

### 环境修复
- gstack browse 启动报 `Target page context or browser has been closed` 失败。原因：macOS 上 Playwright chromium 的 helper 二进制（chrome_crashpad_handler / GPU helper）解压后 mode=600 缺 +x 位。`chmod -R u+x /Volumes/SSD/cache/ms-playwright/chromium-*/chrome-*/Contents/MacOS/*` 与 `*Helpers/*` 后恢复。
- 浏览器进程被沙箱挡掉，加上 `GSTACK_CHROMIUM_NO_SANDBOX=1` 后能正常启动。
- 已把这条经验写到 `~/.gstack/projects/ai-bi/learnings.jsonl`。

### 审计结论
- Design Score **B+** / AI Slop Score **A**（零紫色 / 零 3 列网格 / 零装饰 blob / 零通用 hero 文案）
- 优点：烘焙暖色调一致、Inter Tight + Fraunces + JetBrains Mono 三层字体系统、74px 衬线 H2 招牌时刻、tabular-nums 数字栏、性能 1.84s 零 console error
- 问题：46 个 H3 等大（标题层级扁平）、19/65 触达尺寸不达 WCAG、日期段 tablist 缺 ARIA、桌面侧栏 200px 浪费、移动首屏被品牌块吃掉、88px 天气大标签语义与展示混在一起
- 完整报告：`.gstack/design-reports/design-audit-127-0-0-1-8600-2026-08-28/design-audit.md` + 12 张证据截图 + `design-baseline.json`

### 状态
- 审计落地，**未进入修复阶段**（Fix Loop）。用户选了「审计 + 自动修复」，但建议先 review 报告再确认优先修哪几条。当前 git tree 干净。
